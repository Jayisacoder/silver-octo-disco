# Document environment variable NAMES only.
# Never commit real secret values.

# EXAMPLE_SERVER_SECRET=
# NEXT_PUBLIC_EXAMPLE_PUBLIC_VALUE=

# Add the variable NAMES required by your chosen Prisma/database setup,
# Google OAuth library (client ID/secret), and session configuration.
# Keep values blank here and document local/Vercel setup in your instructions.
